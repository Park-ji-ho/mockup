# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(및 기타 AI 에이전트)를 위한 가이드다.

## 프로젝트 개요

포트폴리오 목업 사이트. 순수 정적 HTML/CSS/JS로 작성하고 GitHub Pages로 배포한다.
빌드 단계가 없으며, `main` 브랜치의 루트가 그대로 배포된다.

## 디자인 시스템 (필수)

- **모든 UI 작업은 [`DESIGN.md`](DESIGN.md)의 디자인 시스템을 따른다.** 색상·타이포·간격·라운드·
  그림자·모션·컴포넌트 규칙은 반드시 DESIGN.md에 정의된 토큰과 원칙을 사용한다.
- 임의의 색상 hex·폰트 크기·여백 값을 직접 쓰지 않는다. DESIGN.md의 토큰(또는 그에 매핑된
  CSS 변수)만 사용한다. 필요한 토큰이 없으면 먼저 DESIGN.md에 정의를 추가한 뒤 사용한다.
- 이 디자인 시스템은 **Pleos Connect UI Design Guide**(자동차 IVI 시스템)를 웹/모바일 목업에
  맞게 적응(adapt)한 것이다. 출처·적응 원칙은 DESIGN.md 상단을 참고한다.
- 목업은 **Scenario Showcase 패턴**(DESIGN.md §5)으로 구성한다: 좌측 시나리오 리모컨 + 웹/앱
  프레임 동시 프리뷰. 프리뷰 레이아웃은 뷰포트 `@media`가 아니라 프레임의 `@container`로 분기하고,
  화면 마크업은 `SCREENS[step](state)` 단일 함수로 정의해 두 프레임에 같은 HTML을 주입한다.

## 핵심 원칙

- **빌드 도구를 추가하지 않는다.** 번들러·트랜스파일러·프레임워크 도입은 사용자 승인 후에만.
- 외부 라이브러리 의존을 최소화한다. 꼭 필요하면 CDN 링크보다 로컬 `assets/`에 포함하는 것을 우선 검토한다.
- 모든 경로는 상대 경로로 작성한다. (GitHub Pages는 `/mockup/` 하위 경로에 배포됨)
- **Figma 임포트 친화적으로 작성한다.** 목업은 `html.to.design` 계열 플러그인으로 Figma에
  가져오므로, 시맨틱 태그·명확한 클래스명·Flexbox/Grid 레이아웃·SVG 아이콘·CSS 변수를
  사용한다. (자세한 절차는 README "Figma로 가져오기" 참고)

## 코드 스타일

- HTML: 시맨틱 태그 사용(`header`, `nav`, `main`, `section`, `footer`). 들여쓰기 2칸.
- CSS: 클래스 기반. 색상·간격 등 반복 값은 `:root` CSS 변수로 관리.
- JS: `const`/`let`만 사용(`var` 금지). 함수는 작고 단일 책임으로.
- 파일/폴더명은 소문자-케밥-케이스(`kebab-case`).

## 작업 규칙

- 기존 파일의 구조·네이밍·컨벤션을 먼저 파악하고 그에 맞춘다.
- 변경은 요청 범위로 한정한다. 요청하지 않은 리팩터링·재구성은 하지 않는다.
- 큰 변경 전에는 계획을 먼저 공유한다.

## 커밋 / 배포

- 커밋 메시지는 명령형·간결하게. 하나의 논리적 변경 = 하나의 커밋.
- `push`나 `commit`은 사용자가 요청할 때만 수행한다.
- 배포는 `main` push로 자동 반영되므로, `main`은 항상 동작하는 상태를 유지한다.

## 검증

- 변경 후에는 로컬 정적 서버(`python3 -m http.server`)로 실제 렌더링을 확인한다.
- 콘솔 에러가 없는지, 반응형(모바일/데스크톱)이 깨지지 않는지 확인한다.
