# AGENTS.md

이 저장소에서 동작하는 코딩 에이전트를 위한 규칙이다. (CLAUDE.md와 동일한 원칙을 따른다.)

## 프로젝트

포트폴리오 목업 — 순수 정적 HTML/CSS/JS, 빌드 없음, GitHub Pages 배포.

## 개발 환경

- 설치할 의존성 없음. 저장소를 클론하면 바로 작업 가능.
- 로컬 확인: `python3 -m http.server 8000` → `http://localhost:8000`

## 디자인 시스템 (필수)

- **모든 UI는 [`DESIGN.md`](DESIGN.md)를 따른다.** 색상·타이포·간격·라운드·그림자·모션·컴포넌트는
  DESIGN.md의 토큰/원칙만 사용한다. 임의의 hex·폰트 크기·여백을 직접 쓰지 않는다.
- DESIGN.md는 **Pleos Connect UI Design Guide**(자동차 IVI)를 웹/모바일에 맞게 적응한 것이다.
- 필요한 토큰이 없으면 DESIGN.md에 먼저 정의를 추가한 뒤 사용한다.

## 규칙

- 빌드 도구·프레임워크·번들러를 임의로 추가하지 않는다.
- 경로는 항상 상대 경로. HTML은 시맨틱 태그, CSS는 클래스/변수 기반, JS는 `const`/`let`.
- 파일·폴더명은 kebab-case.
- 변경은 요청 범위로 한정하고, 기존 컨벤션을 우선한다.

## 커밋

- 명령형·간결한 메시지, 하나의 논리적 변경 = 하나의 커밋.
- 접두어 권장: `feat`, `fix`, `style`, `docs`, `refactor`, `chore`.
- `commit`/`push`는 사용자 요청 시에만.

## 검증 체크리스트

- [ ] DESIGN.md 토큰만 사용(임의 hex·폰트 크기·여백 없음)
- [ ] 로컬 서버에서 정상 렌더링
- [ ] 브라우저 콘솔 에러 없음
- [ ] 모바일/데스크톱 반응형 정상
- [ ] `main`은 항상 배포 가능한 상태
